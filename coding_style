Coding style conventions to be adopted in Phatbooks


// Conditional statements

if (some_condition) this_is_acceptable();

if (some_condition)
{
	this_is_acceptable();
}

if (some_condition)
{
	this_is_acceptable();
	and_so_is_this();
}


// Function definitions

int some_function(int x)
{
	do_function_body();
	return x * 2;
}


// Function calls 

int x = some_function_call(3, y, some_other_thing(y));

int x = some_quite_long_function_call
(	with,
	lots,
	of,
	arguments_that_don't_fit_on,
	one_line
);


// Naming

ClassName
function_name
local_variable_name
local_constant_name
file_name
GUARD_header_guard_hpp
m_member_variable
s_static_class_variable
p_parameter_if_required_to_distinguish_from_member_variable
EnumName
StructName
MACRO
Thing  // Name of class
things  // name of SQL table
thing_id  // name of primary key







